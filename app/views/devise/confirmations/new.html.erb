<div class="h-screen flex w-full">
	<div class="hidden lg:flex w-full lg:w-1/2 login_img_section justify-around items-center">
		<div class="  bg-black opacity-20 inset-0 z-0">
		</div>
		<div class="flex w-full mx-auto px-20 flex-col items-center space-y-6">
			<h1 class="text-white font-bold text-4xl font-sans">Crypto Guard</h1>
			<p class="text-white mt-1">Your No. 1 crypto Helper</p>
			<div class="flex justify-center lg:justify-start mt-6">
				<%= link_to "Get Started", new_registration_path(resource_name), class: "hover:bg-indigo-700 hover:text-white hover:-translate-y-1 transition-all duration-500 bg-white text-indigo-800 mt-4 px-4 py-2 rounded-md font-bold mb-2" %>
			</div>
		</div>
	</div>
	<div class="flex w-full justify-center lg:w-1/2 items-center bg-white space-y-8">
		<%= render "devise/shared/error_messages", resource: resource %>
		<% if flash[:notice] %>
			<div class="fixed flex justify-around  w-full lg:w-1/2 top-0  p-4 mb-4 text-sm text-blue-800  bg-blue-50 dark:bg-gray-800 dark:text-blue-400" role="alert">
				<%= notice %>
			</div>
		<% end %>
		<% if flash[:alert] %>
			<div class="fixed flex justify-around  w-full lg:w-1/2 top-0  p-4 mb-4 text-sm text-red-800  bg-red-50 dark:bg-gray-800 dark:text-red-400" role="alert">
				<%= alert %>
			</div>
		<% end %>
		<div class="w-[70%] px-8">
			<h1 class="text-center text-gray-800 font-bold text-2xl mb-1">Didn't receive confirmation email?</h1>
			<p class="text-center text-sm font-normal text-gray-600 mb-8">Enter Email to resend confirmation instructions</p>
			<%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>
				<div class="flex items-center border-2 mb-8 py-2 px-3 rounded-md">
					<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
						<path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M16 12a4 4 0 10-8 0 4 4 0 008 0zm0 0v1.5a2.5 2.5 0 005 0V12a9 9 0 10-9 9m4.5-1.206a8.959 8.959 0 01-4.5 1.207" />
					</svg>
					<%= f.email_field :email, autofocus: true, autocomplete: "email", class: "pl-2 w-full outline-none border-none", placeholder: "Email Address" %>
				</div>
				<%= f.submit "Resend confirmation instructions", class: "block w-full bg-indigo-600 mt-5 py-3 rounded-md hover:bg-indigo-700 hover:-translate-y-1 transition-all duration-500 text-white font-semibold mb-2" %>
			<% end %>
			<div class="flex justify-between mt-4">
				<%= link_to "Already have an account?", new_session_path(resource_name), class: "text-sm ml-2 hover:text-blue-500 cursor-pointer hover:-translate-y-1 duration-500 transition-all" %>
				<%= link_to "Don't have an account yet?", new_registration_path(resource_name), class: "text-sm ml-2 hover:text-blue-500 cursor-pointer hover:-translate-y-1 duration-500 transition-all" %>
			</div>
		</div>
	</div>
</div>
